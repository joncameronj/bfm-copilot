# Start Frontend Dev Server

Start the Next.js frontend for local development.

## Instructions

Run the frontend server as a background task using the Bash tool with `run_in_background: true`:

```bash
npm run dev
```

After starting, inform the user:
- Frontend: http://localhost:3000
